NODE_ENV=development
PORT=5000

MONGODB_URI=
REDIS_URL=

JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
# 32+ char random strings, e.g: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"

CORS_ORIGIN=

# Optional. Leave unset to log emails instead of sending them (dev default).
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
MAIL_FROM=TaskFlow <no-reply@taskflow.local>